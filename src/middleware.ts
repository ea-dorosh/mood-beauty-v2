// Empty middleware to fix Next.js manifest issue
// This file ensures proper generation of middleware-manifest.json
export function middleware() {
  // No-op middleware
}

export const config = {
  matcher: [],
};
